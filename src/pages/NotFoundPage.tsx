import BlogTemplate from "../templates/BlogTemplate";

export default function NotFoundPage() {
  return (
    <BlogTemplate>
      <div className="text-center font-semibold text-lg mt-20">Void</div>
    </BlogTemplate>
  );
}
